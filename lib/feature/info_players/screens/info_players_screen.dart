import 'package:flutter/material.dart';

class InfoPlayersScreen extends StatelessWidget {
  const InfoPlayersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Players Screen");
  }
}
